# ============================================================
# FF Drafter — Python dependencies (all free)
# Install with:  pip install -r requirements.txt
# ============================================================

# --- Core data ---
pandas>=2.2
numpy>=1.26
pyarrow>=15            # Parquet read/write for boards & feature tables
scipy>=1.13

# --- Free data sources ---
nflreadpy>=0.1.5       # nflverse: season stats, ID crosswalk, draft picks (maintained successor to nfl_data_py)
requests>=2.32         # ESPN auction values, CollegeFootballData API
beautifulsoup4>=4.12   # FantasyPros / news HTML parsing
lxml>=5.2              # fast parser for BeautifulSoup

# --- Modeling ---
scikit-learn>=1.5      # HistGradientBoostingRegressor (projection + quantile floor/ceiling)
scipy>=1.13            # rank correlation in backtest

# --- Bounded narrative nudge (lightweight, free sentiment) ---
vaderSentiment>=3.3

# --- App ---
streamlit>=1.36        # live draft-day dashboard

# --- Config / secrets ---
python-dotenv>=1.0     # load CFBD_API_KEY from .env
