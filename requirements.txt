# ============================================================
# FF Drafter — APP dependencies (Streamlit Community Cloud installs THIS file)
#
# Fully PINNED to the exact versions the app is verified against (pip freeze
# of the working venv). The cloud re-resolves dependencies on EVERY reboot;
# unpinned ranges let a fresh release with a broken native wheel (numpy /
# pandas / pyarrow / protobuf / ...) take the app down with an import-time
# segfault — which is exactly what happened on 2026-07-13.
#
# Deploy with Python 3.13 in the app's Advanced settings (verified on 3.14
# locally). To bump versions: upgrade the local venv, run
#   python scripts/sanity_threat.py    (and the app once)
# then re-freeze here. Pipeline/training extras live in
# requirements-pipeline.txt — the cloud must NOT install those.
# ============================================================
altair==6.2.2
anyio==4.14.1
attrs==26.1.0
blinker==1.9.0
cachetools==7.1.4
certifi==2026.6.17
charset-normalizer==3.4.9
click==8.4.2
gitdb==4.0.12
GitPython==3.1.50
h11==0.16.0
httptools==0.8.0
idna==3.18
itsdangerous==2.2.0
Jinja2==3.1.6
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
MarkupSafe==3.0.3
narwhals==2.23.0
numpy==2.5.1
packaging==26.2
pandas==3.0.3
pillow==12.3.0
protobuf==7.35.1
pyarrow==24.0.0
pydeck==0.9.3
python-dateutil==2.9.0.post0
python-multipart==0.0.32
referencing==0.37.0
requests==2.34.2
rpds-py==2026.6.3
six==1.17.0
smmap==5.0.3
starlette==1.3.1
streamlit==1.59.1
tenacity==9.1.4
toml==0.10.2
typing_extensions==4.16.0
urllib3==2.7.0
uvicorn==0.51.0
websockets==16.0

# Not in the venv freeze, but required if the news-nudge cache is ever absent
# (narrative.fetch_nudges falls back to scoring live headlines). Pure Python.
vaderSentiment==3.3.2
