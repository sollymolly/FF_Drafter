# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Virtual envs
.venv/
venv/
env/

# Secrets
.env

# Generated data & live draft session (keep the folders, ignore contents)
data/raw/*
data/processed/*
data/board/*
data/session.json
data/session.json.tmp
!**/.gitkeep

# ... except the small cached inputs the live app needs at runtime
# (lets Streamlit Community Cloud serve the app without running any scripts)
!data/raw/espn_market_*.parquet
!data/raw/news_nudges.parquet
!data/processed/projections_*.parquet
!data/processed/blend_weights.json
!data/processed/rookie_cards_*.parquet

# OS / editor
.DS_Store
.idea/
.vscode/
